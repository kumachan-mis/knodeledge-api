post:
  tags:
    - Chapters
  operationId: list
  summary: Get list of chapters for a project
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../schemas/ChapterListRequest.yaml
  responses:
    "200":
      description: OK - Returns list of chapters for a project
      content:
        application/json:
          schema:
            $ref: ../../schemas/ChapterListResponse.yaml
    "400":
      description: Bad Request - Invalid request
      content:
        application/json:
          schema:
            $ref: ../../schemas/ChapterListErrorResponse.yaml
    "500":
      description: Internal Server Error - Server error
      content:
        application/json:
          schema:
            $ref: ../../schemas/ApplicationErrorResponse.yaml
